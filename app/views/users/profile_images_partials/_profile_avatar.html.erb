<img id="profile-avater"<% if @user.avatar.attached? %>
  <%= image_tag @user.avatar, height: 350, width: 350 %>
<% else %>
  <%= image_tag gravatar_image_url(@user.email, size: 75), height: 100, width: 100  %>
<% end %></img>
